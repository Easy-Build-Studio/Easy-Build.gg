import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
  output: "export",
  reactCompiler: true,
};

module.exports = {
  output: 'export',
  basePath: '/Easy-Build.gg',
};

export default nextConfig;
